{
  "name": "@serbanghita-gamedev/quadtree",
  "description": "Quadtree",
  "keywords": ["quadtree"],
  "version": "1.0.0",
  "author": "Serban Ghita <serbanghita@gmail.com> (https://ghita.org)",
  "license": "MIT",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "main": "./src/index.ts",
  "scripts": {
    "build": "esbuild ./src/index.ts --bundle --outfile=dist/quadtree.js",
    "demo": "esbuild ./src/demo.ts --bundle --sourcemap --watch --outfile=demo/demo.js --servedir=demo",
    "test": "vitest --globals",
    "lint": "eslint ./src"
  }
}
