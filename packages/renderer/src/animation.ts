import { Animation, SpriteSheet, SpriteSheetAnimation } from "@serbanghita-gamedev/component";
import { Entity } from "@serbanghita-gamedev/ecs";

// export function getDefaultAnimationName(animations: ISpriteSheetAnimation[]): string
// {
//     try {
//         return (animations.find((animationFrame) => animationFrame.defaultAnimation) as ISpriteSheetAnimation)['name'];
//     } catch (e) {
//         throw Error('Your animations file does not contain a "default" animation. Please set "defaultAnimation": true property on one of the animations.');
//     }
// }
